export type TNewRecord = {
  // id: number
  place: string;
  subject: string;
  title: string;
  comment: string;
  dateValue?: string;
  timeValue?: string;
  substationType: string;
  numberOfTP: string;
};
